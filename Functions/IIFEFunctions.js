/* las funciones IIFE o funciones autoejecutadas, son aquellas funciones que se ejecutan inmediatamente después de definirlas:*/

(function IIFE() {
  return console.log("IIFE function");
})();

/* Este patrón se utilizaba antes de ES6 para crear un ámbito de bloque, es decir antes de la llegada de let y const */